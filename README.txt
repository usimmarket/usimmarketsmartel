
Smartel – Netlify Functions package (working Save/Print)

Place the entire `netlify` folder at the root of your repo (same level as index.html).
Add `<script src="/pdf-client.js"></script>` before `</body>`.
In Netlify Build settings set: Publish dir `.` Functions dir `netlify/functions`.
